<section class="page-hero">
  <div class="shell page-hero-content">
    <p class="eyebrow"><%= contactContent.eyebrow %></p>
    <h1><%= contactContent.title %></h1>
    <p><%= contactContent.description %></p>
  </div>
</section>

<section class="section shell contact-layout">
  <div class="contact-sidebar">
    <div class="glass-panel contact-intro" data-reveal>
      <p class="card-kicker">What we can help with</p>
      <div class="feature-grid feature-grid-single">
        <% contactContent.promises.forEach(function (item) { %>
          <article class="feature-card">
            <h3><%= item.title %></h3>
            <p><%= item.description %></p>
          </article>
        <% }) %>
      </div>
    </div>

    <div class="glass-panel contact-direct" data-reveal>
      <p class="card-kicker">Direct contact</p>
      <h2>Prefer to reach us directly?</h2>
      <div class="contact-stack">
        <a href="tel:<%= brand.phone.replace(/[^\d+]/g, '') %>"><%= brand.phone %></a>
        <a href="mailto:<%= brand.email %>"><%= brand.email %></a>
        <p><strong>Main Branch:</strong><br>Kailashpuri colony near bhagat chauraha, Taramandal, Gorakhpur</p>
      </div>
      <div style="margin-top: 1.25rem; border-radius: var(--radius-md); overflow: hidden; border: 1px solid var(--line); line-height: 0;">
        <iframe 
          src="https://maps.google.com/maps?q=Kailashpuri%20colony%20near%20bhagat%20chauraha%20taramandal%20gorakhpur&t=&z=14&ie=UTF8&iwloc=&output=embed" 
          width="100%" 
          height="200" 
          style="border:0;" 
          allowfullscreen="" 
          loading="lazy">
        </iframe>
      </div>
    </div>
  </div>

  <div class="glass-panel contact-form-panel" data-reveal>
    <p class="card-kicker">Send a message</p>
    <h2>We will route your request to the right team.</h2>

    <div style="margin: 1.25rem 0 1.5rem 0; padding: 1.25rem; border-radius: var(--radius-md); background: rgba(99, 102, 241, 0.08); border: 1px solid var(--line); display: flex; justify-content: space-between; align-items: center; gap: 1rem; flex-wrap: wrap;">
      <div style="display: grid; gap: 0.25rem;">
        <span style="font-size: 0.95rem; font-weight: 700; color: var(--text);">Track existing message</span>
        <span style="font-size: 0.85rem; color: var(--text-muted);">If you contacted us without logging in, track your resolution here.</span>
      </div>
      <a href="/contact/status" class="button button-secondary" style="min-height: 38px; padding: 0 16px; font-size: 0.85rem; font-weight: 700;">Check Status</a>
    </div>

    <%- include("partials/notice") %>

    <form class="contact-form" action="/contactus" method="POST">
      <div class="form-row">
        <label class="field-group">
          <span>Full name</span>
          <input type="text" name="fullname" value="<%= formData.fullname %>" required>
        </label>

        <label class="field-group">
          <span>Email address</span>
          <input type="email" name="email" value="<%= formData.email %>" required>
        </label>
      </div>

      <div class="form-row">
        <label class="field-group">
          <span>Phone number</span>
          <input type="tel" name="phone" value="<%= formData.phone %>">
        </label>

        <label class="field-group">
          <span>Subject</span>
          <input type="text" name="subject" value="<%= formData.subject %>">
        </label>
      </div>

      <label class="field-group">
        <span>Your message</span>
        <textarea name="message" rows="6" required><%= formData.message %></textarea>
      </label>

      <button type="submit" class="button button-primary button-block">Send message</button>
    </form>
  </div>
</section>
