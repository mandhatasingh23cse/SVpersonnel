<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= typeof title !== "undefined" ? title : "SV Personnels - Govt. Authorized Job & Hiring Provider" %></title>
  <meta
    name="description"
    content="<%= brand && brand.tagline ? brand.tagline : 'SV Personnels connects employers with verified manpower and job seekers across India.' %>"
  />
  <meta name="theme-color" content="#16203a" />
  <link rel="icon" href="/assets/svpersonnel-logo.png" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=Sora:wght@500;600;700&display=swap"
    rel="stylesheet"
  >
  <link rel="stylesheet" href="/style.css">
</head>
<body class="<%= pageClass || '' %>">
  <canvas id="canvas-3d-background" style="position: fixed; top: 0; left: 0; width: 100vw; height: 100vh; pointer-events: none; z-index: 0;"></canvas>
  <%- include("../partials/header") %>

  <main class="site-main">
    <%- body %>
  </main>

  <%- include("../partials/footer") %>

  <script src="/javascript/site.js" defer></script>
  <script src="/javascript/custom-skills-select.js" defer></script>
  <script src="/javascript/location-picker.js" defer></script>
  <script src="/javascript/interactive-pincode-modal.js" defer></script>
  <% if (typeof scripts !== "undefined") { %>
    <% scripts.forEach(function (src) { %>
      <script src="<%= src %>" defer></script>
    <% }) %>
  <% } %>
</body>
</html>
