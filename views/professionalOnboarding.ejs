<section class="section shell auth-layout" style="padding-top: 3rem; padding-bottom: 5rem;">
  <div class="glass-panel auth-story" data-reveal style="max-width: 420px;">
    <p class="eyebrow">Onboarding Steps</p>
    <h1>Welcome, <%= user && user.name ? user.name : 'Partner' %>!</h1>
    <p>Let's complete your professional setup so clients and employers can easily find you across India.</p>

    <ul class="check-list" style="margin-top: 1.5rem;">
      <li>Select all your specific skills across 15 categories</li>
      <li>Set your preferred service city</li>
      <li>Choose full-time, part-time, or flexible availability</li>
      <li>Specify your charges (which clients can negotiate with you)</li>
    </ul>

    <div class="glass-inset" style="margin-top: 2rem; padding: 1rem; border-left: 4px solid var(--color-brand); background: rgba(167, 139, 250, 0.08); border-radius: 8px;">
      <p style="font-size: 0.9rem; margin: 0; color: #fff;">
        💡 <strong>Pro Tip:</strong> Ticking more skills in your category increases your visibility when clients search or post work requirements!
      </p>
    </div>
  </div>

  <div class="glass-panel auth-form-card" data-reveal style="flex: 1; max-width: 760px;">
    <p class="card-kicker">Professional Skills & Rates</p>
    <h2>Customize your service profile.</h2>

    <form class="auth-form onboarding-form" action="/professional/onboarding" method="POST">
      <!-- 1. Select Skills by Ticking Checkboxes -->
      <div class="field-group" style="margin-bottom: 2rem;">
        <span style="font-size: 1.1rem; font-weight: 700; color: #fff; margin-bottom: 0.5rem; display: block;">1. Select Your Skills (Tick all that apply)</span>
        <p style="font-size: 0.85rem; color: var(--color-ink-light); margin-top: 0; margin-bottom: 1rem;">Expand any category below and tick the exact roles and services you offer.</p>
        
        <div class="onboarding-skills-accordion" style="display: flex; flex-direction: column; gap: 0.75rem; max-height: 420px; overflow-y: auto; padding-right: 0.5rem;">
          <% if (ottCategories && ottCategories.length) { %>
            <% ottCategories.forEach(function(cat, idx) { %>
              <details class="glass-inset" style="border-radius: 12px; padding: 0.75rem 1rem; border: 1px solid rgba(255,255,255,0.1); background: rgba(15, 23, 42, 0.6);" <%= idx === 0 ? 'open' : '' %>>
                <summary style="font-weight: 700; cursor: pointer; display: flex; align-items: center; justify-content: space-between; color: #fff; font-size: 0.95rem;">
                  <span><%= cat.headerIcon || '🔹' %> <%= cat.title %></span>
                  <span style="font-size: 0.75rem; opacity: 0.7; background: rgba(255,255,255,0.1); padding: 2px 8px; border-radius: 10px;"><%= (cat.items || []).length %> skills</span>
                </summary>
                <div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(220px, 1fr)); gap: 0.6rem; margin-top: 0.85rem; padding-top: 0.75rem; border-top: 1px solid rgba(255,255,255,0.08);">
                  <% (cat.items || []).forEach(function(item) { %>
                    <label style="display: flex; align-items: flex-start; gap: 0.5rem; cursor: pointer; font-size: 0.88rem; color: var(--color-ink); background: rgba(255,255,255,0.03); padding: 0.5rem; border-radius: 8px; border: 1px solid rgba(255,255,255,0.05); transition: all 0.2s;">
                      <input type="checkbox" name="skills" value="<%= item.name %>" style="margin-top: 2px; accent-color: var(--color-brand); width: 16px; height: 16px;">
                      <div>
                        <strong style="color: #fff; display: block; line-height: 1.2;"><%= item.name %></strong>
                        <small style="color: var(--color-ink-light); font-size: 0.75rem;"><%= item.desc %></small>
                      </div>
                    </label>
                  <% }) %>
                </div>
              </details>
            <% }) %>
          <% } %>
        </div>
      </div>

      <!-- 2. Service Location / Pincode -->
      <div class="form-row" style="display: grid; grid-template-columns: repeat(auto-fit, minmax(min(200px, 100%), 1fr)); gap: 1rem; margin-bottom: 1.5rem;">
        <label class="field-group">
          <span>2. Pincode (Auto-fetch City)</span>
          <div style="position: relative;">
            <input type="text" name="pincode" id="pro_pincode" maxlength="6" value="<%= user && user.pincode ? user.pincode : '' %>" placeholder="6-digit Pincode (e.g. 226006)" required class="form-input" style="width: 100%; padding: 0.75rem 1rem; border-radius: 12px; background: rgba(15, 23, 42, 0.8); color: #fff; border: 1px solid rgba(255,255,255,0.15);">
            <span id="pro_pincode_status" style="position: absolute; right: 10px; top: 12px; font-size: 0.8rem; color: #38bdf8;"></span>
          </div>
          <small class="field-hint">We fetch your exact city and area from India Post.</small>
        </label>

        <label class="field-group">
          <span>Service City / District</span>
          <input type="text" name="city" id="pro_city" value="<%= user && user.city ? user.city : '' %>" placeholder="City (e.g., Lucknow, Gurugram)" required class="form-input" style="width: 100%; padding: 0.75rem 1rem; border-radius: 12px; background: rgba(15, 23, 42, 0.8); color: #fff; border: 1px solid rgba(255,255,255,0.15);">
          <small class="field-hint">City where you provide services.</small>
        </label>

        <label class="field-group">
          <span>Locality / Area</span>
          <input type="text" name="area" id="pro_area" list="pro-localities" value="<%= user && user.area ? user.area : '' %>" placeholder="Locality / Sector / Colony" required class="form-input" style="width: 100%; padding: 0.75rem 1rem; border-radius: 12px; background: rgba(15, 23, 42, 0.8); color: #fff; border: 1px solid rgba(255,255,255,0.15);">
          <datalist id="pro-localities"></datalist>
          <small class="field-hint">Select or type your exact area.</small>
        </label>

        <!-- 3. Work Type (Part Time, Full Time, Both) -->
        <label class="field-group">
          <span>3. Work Availability</span>
          <select name="jobType" required class="form-input" style="width: 100%; padding: 0.75rem 1rem; border-radius: 12px; background: rgba(15, 23, 42, 0.8); color: #fff; border: 1px solid rgba(255,255,255,0.15); cursor: pointer;">
            <option value="Both / Flexible">Both (Part Time & Full Time)</option>
            <option value="Full Time">Full Time Only</option>
            <option value="Part Time">Part Time Only</option>
          </select>
          <small class="field-hint">Are you open to part-time, full-time, or both?</small>
        </label>
      </div>

      <!-- 4. Charges & Negotiation Note -->
      <div class="field-group glass-inset" style="padding: 1.25rem; border-radius: 14px; background: rgba(255,255,255,0.03); border: 1px dashed rgba(167, 139, 250, 0.4); margin-bottom: 2rem;">
        <div style="display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 1rem;">
          <div style="flex: 1; min-width: 220px;">
            <span style="font-weight: 700; color: #fff; display: block; margin-bottom: 0.35rem;">4. Your Charges / Rate (₹)</span>
            <input type="number" name="charges" min="0" step="50" placeholder="e.g. 500 or 15000" required class="form-input" style="width: 100%; padding: 0.75rem 1rem; border-radius: 10px; background: rgba(15, 23, 42, 0.9); color: #fff; border: 1px solid rgba(255,255,255,0.2); font-size: 1.05rem; font-weight: 600;">
            <small class="field-hint">Enter your expected charges per day / task / month.</small>
          </div>
          
          <div style="flex: 1.2; min-width: 240px; background: linear-gradient(135deg, rgba(167, 139, 250, 0.15), rgba(59, 130, 246, 0.15)); padding: 1rem; border-radius: 12px; border: 1px solid rgba(167, 139, 250, 0.3);">
            <div style="display: flex; align-items: center; gap: 0.5rem; color: #a78bfa; font-weight: 700; font-size: 0.95rem; margin-bottom: 0.25rem;">
              <span>🤝 Client Can Negotiate</span>
              <span style="background: #a78bfa; color: #000; font-size: 0.65rem; padding: 2px 6px; border-radius: 4px; text-transform: uppercase;">Enabled</span>
            </div>
            <p style="font-size: 0.82rem; color: var(--color-ink); margin: 0; line-height: 1.4;">
              As per platform policy, your listed charges serve as a baseline. <strong>Client can negotiate that price</strong> directly with you before confirming any work contract or booking.
            </p>
          </div>
        </div>
      </div>

      <button type="submit" class="button button-primary button-block" style="padding: 1rem; font-size: 1.05rem; font-weight: 700; border-radius: 12px;">
        Complete Onboarding & Go to Dashboard ›
      </button>
    </form>
  </div>
  <script>
    document.getElementById("pro_pincode").addEventListener("input", async function() {
      const pin = this.value.trim();
      const statusEl = document.getElementById("pro_pincode_status");
      const datalist = document.getElementById("pro-localities");
      if (!pin || pin.length !== 6 || isNaN(pin)) {
        statusEl.textContent = "";
        return;
      }
      statusEl.textContent = "⌛ Fetching...";
      try {
        const res = await fetch(`https://api.postalpincode.in/pincode/${pin}`);
        const data = await res.json();
        if (data && data[0] && data[0].Status === "Success" && data[0].PostOffice) {
          const pos = data[0].PostOffice;
          const firstPo = pos[0];
          document.getElementById("pro_city").value = firstPo.District || firstPo.Division || firstPo.Block || "";
          datalist.innerHTML = "";
          pos.forEach(po => {
            const opt = document.createElement("option");
            opt.value = po.Name;
            datalist.appendChild(opt);
          });
          if (!document.getElementById("pro_area").value && pos.length > 0) {
            document.getElementById("pro_area").value = pos[0].Name;
          }
          statusEl.textContent = "✅ Valid";
        } else {
          statusEl.textContent = "❌ Not found";
        }
      } catch (e) {
        statusEl.textContent = "";
      }
    });
  </script>
</section>
